class ClaimParamsAll {
  final int categoryId;
  final String gameId;
  final int fromDate;
  final int toDate;

  ClaimParamsAll(
      {required this.categoryId,
      required this.gameId,
      required this.fromDate,
      required this.toDate});
}
